# One or more GitHub personal access tokens (comma-separated for rotation)
GITHUB_TOKENS=ghp_xxx,ghp_yyy
# Or use a single token:
# GITHUB_PERSONAL_ACCESS_TOKEN=ghp_xxx

# Optional: default search query (used when you run "node index.js" with no args)
# QUERY=language:javascript repos:>1 followers:50..100

# Optional: when running with no args, page range (default: 1 1)
# START_PAGE=1
# END_PAGE=5

# Optional: path for the JSON output file (default: data/users.json)
# OUTPUT_PATH=data/users.json
